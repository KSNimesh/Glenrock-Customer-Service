<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ksnimesh.theglenrock">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".bottom"
            android:label="@string/title_activity_bottom" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".top_toolbar" />
        <activity android:name=".about_hotel" />
        <activity android:name=".Reservation" />
        <activity android:name=".Travel_Guide" />
        <activity android:name=".Facilities" />
        <activity android:name=".Connectivity" />
        <activity android:name=".Sign_In" />
        <activity android:name=".SignUp" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <!-- <meta-data -->
        <!-- android:name="com.google.android.geo.API_KEY" -->
        <!-- android:value="AIzaSyBkv0A1t6icf7MO2y1SfZL1Y6c8Qaw_xnM" /> -->


        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity1"
            android:label="Maps1" />
        <activity
            android:name=".MapsActivity2"
            android:label="@string/title_activity_maps2" />
        <activity android:name=".Tabs.TabActivity" />
        <activity
            android:name=".FoodnavHome"
            android:label="@string/title_activity_foodnav_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Foodlist" />
        <activity android:name=".LandmarkPlaces.Landmark_detail01" />
        <activity
            android:name=".MapsActivity3"
            android:label="@string/title_activity_maps3" />
        <activity
            android:name=".MapsActivity4"
            android:label="@string/title_activity_maps4" />
        <activity android:name=".FoodDetail" />
        <activity android:name=".Cart" />
        <activity android:name=".Cart_Layout" />
        <activity android:name=".HomeMain" />
        <activity
            android:name=".Botomnavigation1"
            android:label="@string/title_activity_botomnavigation1"></activity>
    </application>

</manifest>